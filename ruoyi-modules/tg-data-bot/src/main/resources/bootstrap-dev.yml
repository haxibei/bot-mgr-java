# Spring
spring:
  cloud:
    nacos:
      username: nacos
      password: nacos_Ba88kOgi3PP8NEw
      discovery:
        # 服务注册地址
        server-addr: 127.0.0.1:8848
        # 配置所属的命名空间
        namespace: ${spring.profiles.active}
      config:
        # 配置中心地址
        server-addr: 127.0.0.1:8848
        # 配置所属的命名空间
        namespace: ${spring.profiles.active}
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-bot-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
