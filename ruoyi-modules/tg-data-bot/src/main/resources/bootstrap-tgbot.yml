# Spring
spring:
  cloud:
    nacos:
      username: nacos
      password: nacos_Aa46kOgi2I33NkwG3Y
      discovery:
        # 服务注册地址
        server-addr: 104.152.208.59:18848
        # 配置所属的命名空间
        namespace: ${spring.profiles.active}
      config:
        # 配置中心地址
        server-addr: 104.152.208.59:18848
        # 配置所属的命名空间
        namespace: ${spring.profiles.active}
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
